# Truss

A good base to start new P2P project, already with needed code infrastructure

## Building

At first you need to install all dependencies, you can just run setup script:

```sh
./setup/full_setup.sh
```

And than run this command:

```sh
axle build .
```

## Usage

Nothing works now, in development
