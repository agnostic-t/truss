{
  "metadata": {
    "name": "truss",
    "version": "0.0.0"
  },

  "dependencies": {
    "main": { "version": ">=0.0.0" },
    "linkserv": { "version": ">=0.0.0" }
  },

  "only-deps": true,
  "defaults": "defaults.json",
  "target": "debug"
}
